spring:
    application:
        name: spring-aperture-simple-api
    data:
        mongodb:
            uri: mongodb://localhost:27017/apiApertureTest
            host: localhost
            port: 27017
            database: apiApertureTest
    output:
            ansi:
                enabled: always

security:
    basic:
        enabled: false
server:
    port: 8080

mongeez:
  enabled: true
